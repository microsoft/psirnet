[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "psirnet"
version = "0.0.1"
description = "Rapid-Acquisition Late Enhancement Imaging"
requires-python = ">=3.9"

# pick up everything under src/
[tool.setuptools.packages.find]
where = ["src"]